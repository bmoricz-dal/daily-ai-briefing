name: Daily Source Pack

on:
  schedule:
    - cron: "30 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-source-pack:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run source collection script
        run: python scripts/fetch_sources.py

      - name: Commit and push source pack
        run: |
          git config user.name "daily-briefing-bot"
          git config user.email "daily-briefing-bot@users.noreply.github.com"
          git add data/source_packs/
          git commit -m "Add daily source pack" || echo "No changes to commit"
          git push
